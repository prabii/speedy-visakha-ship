import { Hero } from "@/components/Hero";
import { Services } from "@/components/Services";

const Home = () => {
  return (
    <main className="min-h-screen">
      <Hero />
      <Services />
    </main>
  );
};

export default Home;